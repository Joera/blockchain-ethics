<!DOCTYPE html>
<html lang="{{language}}">
{{> head }}
<body>
    {{> header}}
    <main>

        <section class="medium_container">
            <h1>{{title}}</h1>
            <2>{{custom.tagline}}</2>
        </section>

        <section class="small_container">
            {{{content}}}
        </section>

    </main>
</body>
</html>
